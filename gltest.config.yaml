# Direct tests are in-memory; hosted proof and deployment scripts target Studionet.
networks:
  default: localnet
  localnet:
    url: "http://127.0.0.1:4000/api"
  studionet:
    url: "https://studio.genlayer.com/api"
paths:
  contracts: "contracts"
  artifacts: "test-artifacts"
environment: .env
